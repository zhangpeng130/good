# AGENTS.md

## Cursor Cloud specific instructions

This repository (`zhangpeng130/good`) is currently an empty skeleton. As of the initial setup:

- The only tracked file is `redme.txt` (a placeholder containing `1`).
- There is no application code, no dependency manifests (`package.json`, `requirements.txt`, etc.), no build system, no Docker configuration, and no tests.
- No services need to be started.
- No linting, testing, or build commands are available.

When application code is added to this repository, this section should be updated with:
- How to install dependencies
- How to run, lint, test, and build the application
- Any non-obvious caveats for the development environment
